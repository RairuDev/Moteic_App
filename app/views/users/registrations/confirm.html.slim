h2.page-theme sign_up
= form_with model: @user, url: users_sign_up_path(@user), local: true do |f|
  ul
    li.user_info_list_items 名前：#{session[:name]}
    li.user_info_list_items メールアドレス：#{session[:email]}
    li.user_info_list_items 性別：#{(@user.gender_i18n)}
    li.user_info_list_items 年齢：#{session[:age]}
    li.user_info_list_items 体型：#{session[:body_shape]}
    li.user_info_list_items 職業：#{@user.job_i18n}
    li.user_info_list_items 年収：#{session[:income]}
    li.user_info_list_items 住まい：#{@user.prefecture.name}
  .actions
    = f.submit "戻る", name: :back, class: 'back-btn'
    = f.submit ('決定'), class: 'green-btn'